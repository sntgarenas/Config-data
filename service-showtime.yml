server:
  port: 9003

spring:
  application:
    name: "service-showtime"
  boot:
    admin:
      client:
        url: http://localhost:9011

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"